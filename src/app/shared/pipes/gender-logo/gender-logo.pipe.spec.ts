import { GenderLogoPipe } from './gender-logo.pipe';

describe('GenderLogoPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderLogoPipe();
    expect(pipe).toBeTruthy();
  });
});
